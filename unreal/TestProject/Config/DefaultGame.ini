[/Script/EngineSettings.GeneralProjectSettings]
ProjectName=FigmaBridgeTest
CompanyName=Rocket Science
Description=Host project for testing the Figma Token Bridge plugin.

; ---------------------------------------------------------------------------
; Figma Token Bridge
;
; ProjectId is deliberately absent. The plugin generates one on first editor run
; and writes it back here, which is the behaviour the Figma handshake depends on
; — so leaving it empty means the first run actually exercises that path.
; After the first run this section will also carry ProjectId and ActiveTokens.
;
; TokensFile is relative to this project's directory, so it points back at the
; repo's tokens/ folder: the committed JSON is the thing under review, and the
; importer reads exactly what a reviewer saw.
; ---------------------------------------------------------------------------

[/Script/FigmaTokenBridge.DesignTokenSettings]
ProjectId=B608334A-43F8-7269-83DF-96892C5638C5
FigmaFileKey=FruupulmvbUZbkUlTQ0kIm
FigmaFileName=
bRequireFileKeyMatch=True
bRequireProjectIdMatch=True
TokensFile=(FilePath="../../tokens/tokens.tokens.json")
GeneratedPackagePath=/Game/DesignSystem/Generated
TokensAssetName=DA_DesignTokens
ActiveTokens=/Game/DesignSystem/Generated/DA_DesignTokens.DA_DesignTokens

